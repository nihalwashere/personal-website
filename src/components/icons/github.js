import React from "react";
import GitHub from "../../../public/images/github.svg";

const IconGitHub = ({ width, height, className }) => (
  <GitHub width={width} height={height} className={className} />
);

export default IconGitHub;
