import React from "react";
import Close from "../../../public/images/close.svg";

const IconClose = ({ width, height, className }) => (
  <Close width={width} height={height} className={className} />
);

export default IconClose;
