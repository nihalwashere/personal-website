import React from "react";
import Mail from "../../../public/images/mail.svg";

const IconMail = ({ width, height, className }) => (
  <Mail width={width} height={height} className={className} />
);

export default IconMail;
