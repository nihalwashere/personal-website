import React from "react";
import LinkedIn from "../../../public/images/linkedin.svg";

const IconLinkedin = ({ width, height, className }) => (
  <LinkedIn width={width} height={height} className={className} />
);

export default IconLinkedin;
