import React from "react";
import External from "../../../public/images/external.svg";

const IconExternal = ({ width, height, className }) => (
  <External width={width} height={height} className={className} />
);

export default IconExternal;
