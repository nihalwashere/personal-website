import React from "react";
import Menu from "../../../public/images/menu.svg";

const IconMenu = ({ width, height, className }) => (
  <Menu width={width} height={height} className={className} />
);

export default IconMenu;
